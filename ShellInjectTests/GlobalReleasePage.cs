// A global-namespace view verifies the convention resolver's namespace-free path.
public sealed class GlobalReleasePage : ContentPage;
public sealed class GlobalReleaseViewModel : ShellInject.ShellInjectViewModel;
